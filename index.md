# 视频提取与处理系统

## 目录

1. [项目概述](#项目概述)
2. [技术栈](#技术栈)
3. [系统架构](#系统架构)
4. [核心模块详介](#核心模块详介)
5. [扩展功能](#扩展功能)
6. [错误处理与问题排查](#错误处理与问题排查)
7. [配置参考](#配置参考)
8. [依赖安装](#依赖安装)
9. [快速上手指南](#快速上手指南)
10. [更新记录](#更新记录)

---

## 项目概述

本系统致力于从第三方平台链接中提取视频、音频及字幕，并提供完善的后处理能力。核心下载引擎支持 **1800+ 主流视频平台**，结合 AI 驱动的语音转录和智能摘要，在原始媒体基础上进一步提升内容价值。

### 核心目标

- 从任意支持的第三方 URL 中提取视频文件（MP4）和音频文件（MP3）
- 提取或自动生成视频字幕（VTT / SRT）
- 当平台字幕不可用时，通过 Whisper 提供并行高速语音转录
- 基于转录内容生成结构化 AI 摘要和关键词标签
- 支持批量处理、格式转换、水印去除等扩展能力

> ℹ️ 核心下载引擎支持的平台包括：YouTube、哔哩哔哩、抖音 / TikTok、Twitter/X、Instagram、Vimeo 等。

---

## 技术栈

| 组件 | 技术方案 | 职责 |
|---|---|---|
| 视频下载 | yt-dlp（Python） | 视频 / 音频下载引擎 |
| 媒体处理 | FFmpeg | 音频提取、格式转换、水印去除 |
| 语音转录 | faster-whisper | 离线并行 Whisper 转录 |
| 字幕处理 | yt-dlp + pysrt | 字幕提取与 SRT / VTT 转换 |
| AI 摘要 | Claude API | 转录内容摘要与关键词 |
| 任务编排 | Python asyncio | 流水线调度与重试 |
| 存储 | 本地 / S3 | 结果文件存储 |

---

## 系统架构

### 整体流水线

```text
用户(URL)
   ↓
视频下载 (yt-dlp)
   ↓
字幕提取
   ├─ 成功 → 后处理
   └─ 失败 → Whisper 转录 → 后处理
```

### 输出文件结构

```text
output/
└── <视频标题>/
    ├── video.mp4
    ├── audio.mp3
    ├── subtitle.vtt
    ├── subtitle.srt
    ├── transcript.txt
    ├── summary.md
    ├── keywords.json
    └── info.json
```

---

## 核心模块详介

### 视频下载模块

```bash
python download_video.py <URL> \
  --output-dir ./output \
  --lang zh en \
  --quality best \
  --cookies chrome
```

标准输出（JSON）：

```json
{
  "folder": "./output/Video_Title",
  "video": "video.mp4",
  "audio": "audio.mp3",
  "subtitle": "subtitle.vtt",
  "needs_whisper": false
}
```

> ⚠️ 会员或登录内容需使用 `--cookies chrome`

---

### Whisper 语音转录模块

```bash
python parallel_transcribe.py \
  --input audio.mp3 \
  --output-dir ./output/<title> \
  --model small \
  --language auto \
  --workers 4
```

**模型推荐**：`small`（速度 / 质量最佳平衡）

---

### AI 智能摘要模块

摘要输出结构示例：

```md
# 📹 视频摘要：标题

## 📌 一句话概括

## 🔑 核心要点

## 💬 关键语段

## 📊 主题标签

## ⭐ 整体评价
```

---

## 扩展功能

### 字幕格式转换

```bash
python convert_subtitle.py --input subtitle.vtt --format srt
```

### 关键词提取

```json
{
  "tags": ["AI", "Whisper"],
  "confidence": [0.95, 0.88]
}
```

### 水印去除（实验级）

```bash
python remove_watermark.py --input video.mp4 --output clean.mp4
```

---

## 错误处理与问题排查

| 问题 | 原因 | 解决方案 |
|---|---|---|
| 下载失败 | 未鉴权 | 使用 cookies |
| 无字幕 | 平台不支持 | Whisper 转录 |
| 转录慢 | 模型过大 | 切换 small |

---

## 配置参考

```yaml
output_dir: ./output
whisper:
  model: small
  workers: 4
summary:
  enabled: true
```

---

## 依赖安装

```bash
bash scripts/install_deps.sh
```

---

## 快速上手指南

1. 安装依赖
2. 下载视频
3. Whisper 转录（如需要）
4. 生成摘要


